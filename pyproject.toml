[project]
name = "my-writing-rl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.7.0",
    "torch>=2.10.0",
    "transformers>=5.3.0",
    "verifiers>=0.1.10",
]
